### 1. 다음 빈칸을 채우세요
1) STL 구성 요소의 인터페이스를 변경하는 구성 요소를 (   )라 합니다.
2) 컨테이너의 인터페이스를 변경한 stack, queue, priority_queue 컨테이너를 (   )라 합니다.
     
     1) 어댑터 
     2) 컨테이너 어댑터 

### 2. 다음 빈 칸을 채우세요.
1) stack의 원소 추가하는 멤버함수 :
2) stack의 원소 참조 멤버함수 :
3) stack의 원소 제거 멤버함수 :
4) queue의 원소 추가 멤버함수 :
5) queue의 원소 참조 멤버함수 :
6) queue의 원소 제거 멤버함수 :
7) priority_queue는 힙 알고리즘으로 구현되므로 선택 가능한 컨테이너는 (   )반복자를 지원해야 합니다.

     1) push()
     2) top()
     3) pop()
     4) push()
     5) front(), back()
     6) pop()
     7) 임의 접근 반복자