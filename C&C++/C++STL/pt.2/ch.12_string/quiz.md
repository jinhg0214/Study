### 1. 다음 빈칸을 채우세요

1) string 클래스는 문자들을 저장하는 컨테이너로 C, C++ 처럼 (   ) 문자를 요구하지 않기 떄문에   
    C-Style 문자열로 변환하려면 c_str() 멤버 함수를 사용합니다.
2) c_str() 멤버 함수는 (   )를 반환하며 data() 멤버 함수는 (   ) 를 반환합니다.

     1) '\0' null문자   
     2) c_str()은 '\0' 문자를 포함한 문자열 주소를 반환하며   
        data() 멤버 함수는 '\0'문자 포함을 필수 조건으로 하지 않는 문자열 주소를 반환


### 2. 빈칸

1) string 객체의 찾기 관련 함수가 실패하면 (   )를 반환합니다.
2) string 객체는 vector 처럼 메모리 재할당으로 발생하는 성능저하를 막기 위해   
    예약된 메모리 공간의 크기를 확인하려면 (   ) 멤버 함수를 사용할 수 있고    
    미리 메모리 공간을 예약하려면 (   ) 멤버 함수를 사용할 수 있습니다.

     1) string::npos
     2) capacity(), reserve()

### 3. 다음 함수의 기능을 쓰세요

1) copy()   : 버퍼로 문자열 복사
2) c_str()  : 문자열의 주소를 반환 
3) append() : 덧붙이기
4) replace(): 대체