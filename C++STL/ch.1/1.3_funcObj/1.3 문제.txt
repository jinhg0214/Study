1. 다음 빈 칸을 채우세요

1) 함수처럼 호출 가능한 클래스 객체를 가리켜 (    ) 라 합니다.
2) 함수처럼 호출 가능한 클래스 객체는 (    )연산자를 오버로딩해 생성합니다

/*
함수 객체
() 연산자를
*/

2. 다음 Equal 클래스의 객체가  cmp일 때 두 정수가 같으면 true, 아니면 false를 반환하는 
Equal 클래스를 작성하세요.

    if(cmp(10,20))
        cout << "같다!" << endl;
    else
        cout << "다르다!" << endl;


3. 다음 Adder 클래스의 객체가 add일 때 두 정수의 합을 반환하는 Adder 클래스를 작성하세요